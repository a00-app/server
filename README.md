# How to run?

- Install the packages
- Start IPFS using Docker or `ipfs daemon`
- Start MongoDB and create a database for a00
- Rename .env.template and fill the fields with correct values. You will need to enter your EVM wallet private key, MongoURI, IPFS API Endpoint, and JWT secret for secure authentication & communication.
- Run `npm run dev`
